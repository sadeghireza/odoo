<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_ipmo_amendment_tree" model="ir.ui.view">
        <field name="name">ipmo.agreement.amendment.tree</field>
        <field name="model">ipmo.agreement.amendment</field>
        <field name="arch" type="xml">
            <list>
                <field name="amendment_no" />
                <field name="agreement_id" />
                <field name="amendment_date" />
                <field name="delta_amount" />
                <field name="status" />
            </list>
        </field>
    </record>

    <record id="view_ipmo_amendment_form" model="ir.ui.view">
        <field name="name">ipmo.agreement.amendment.form</field>
        <field name="model">ipmo.agreement.amendment</field>
        <field name="arch" type="xml">
            <form>
                <header>
                    <button name="action_submit" type="object" string="Submit" class="btn-primary" invisible="status != 'draft'" />
                    <button name="action_approve" type="object" string="Approve" invisible="status != 'submitted'" />
                    <field name="status" widget="statusbar" statusbar_visible="draft,submitted,approved" />
                </header>
                <sheet>
                    <div class="oe_button_box" name="button_box">
                        <button name="action_view_attachments" type="object" class="oe_stat_button" icon="fa-paperclip">
                            <field name="attachment_count" widget="statinfo" string="Attachments" />
                        </button>
                    </div>
                    <group>
                        <group>
                            <field name="agreement_id" />
                            <field name="amendment_no" />
                            <field name="amendment_date" />
                        </group>
                        <group>
                            <field name="delta_amount" />
                            <field name="delta_duration" />
                        </group>
                    </group>
                    <group>
                        <field name="reason" />
                    </group>
                </sheet>
            </form>
        </field>
    </record>

    <record id="action_ipmo_agreement_amendment" model="ir.actions.act_window">
        <field name="name">Amendments</field>
        <field name="res_model">ipmo.agreement.amendment</field>
        <field name="view_mode">list,form</field>
    </record>
</odoo>
